services:
  web:
    build:
      context: .
    init: true
    restart: unless-stopped
    # SQLite needs to write the bind-mounted database and its WAL files.
    user: "${TRADING_DEBATE_UID:-1000}:${TRADING_DEBATE_GID:-1000}"
    ports:
      - "8765:8765"
    volumes:
      - ./data:/app/data
