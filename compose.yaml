services:
  web:
    build:
      context: .
    init: true
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8765}:8765"
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
